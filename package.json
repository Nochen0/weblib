{
  "name": "weblib",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "bun tsc --watch src/*.ts --outDir build",
    "serve": "bun --watch index.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
